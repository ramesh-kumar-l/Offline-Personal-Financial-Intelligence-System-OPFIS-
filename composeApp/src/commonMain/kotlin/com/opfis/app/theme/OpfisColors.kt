package com.opfis.app.theme

import androidx.compose.ui.graphics.Color

/**
 * Seed color tokens for the OPFIS design system (SystemPrompt Part 3,
 * "Color System"). Meaning is never carried by color alone - every
 * usage of these tokens must be paired with an icon, text, or shape.
 */
object OpfisColors {
    val ProfessionalBlue = Color(0xFF1F5FBF)
    val Success = Color(0xFF1E8E5A)
    val Warning = Color(0xFFB7791F)
    val Error = Color(0xFFC2432D)
    val InformationNeutralBlue = Color(0xFF3D6FA8)
    val BackgroundNeutralGray = Color(0xFFF5F6F8)
}
